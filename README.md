# GA1.1-teamX

ADD
#This is a function that adds two numbers and give the result.

SUBTRACT
- function subtract(a,b) allows two numbers to be subtracted from each other. The function will subtract b from a and works with integers and floats. The function only works with two inputs and does not accept any more. 
# example:
subtract(1,2) will return -1

- Added dist_from_zero(x) to the subtract file which will show the distance of x from 0.
# example:
dist_from_zero(-12) returns 12.


MULTIPLY
- multiply function added. This can be applicable when two values need to be multiplied together. this function will only work with integers and float values, no strings allowed. 
# example:
multiply(3.1,6) shift+enter
returns float 18.6





